# looky 디자인 전수 정합 체크리스트 (Figma 원본 기준)

> 작성: 2026-06-24 · 기준 Figma: `Design` node `414:13282` (GUI 2차_260624) · 캔버스 390×844 (모바일)
> **방식**: 페이지별 px 단위 대조. 자동 수정 안 함 — 순수 체크리스트만.
> 범례: `- [ ] 🟦 FE` = 프론트 업데이트 / `- [ ] 🟥 DSGN` = 디자이너 수정요청 / `✅` = 정합 확인됨 / `🙋 USER` = 사용자 진행 예정
> 각 항목 형식: **현상** — `코드값` ↔ `Figma값` (근거 노드)

---

## 📌 한눈 요약

**전반적으로 정합도 높음.** 토큰·공통 컴포넌트·F01~F03·F06 완료/실패·결과 게이트·결과 실패는 px 단위로 거의 정확. 디자인팀 요청 4건 중 3건(토스트·키보드·인디케이터) 구현 완료.

**가장 큰 이슈 (의사결정 필요)**
- 🔴 **결과 본문(F05)이 Figma 풀디자인과 구조적으로 다름** — 코드는 간소화(2×2 그리드), Figma는 합성 네컷+종합분석+형용사 태그+팁 카드. 백엔드 제공 범위에 종속 → PM/백엔드/디자이너 합의 필요

**FE 수정 완료 (2026-06-24 · main 직접 · 빌드 통과)**
- [x] F04 제목블록→카드 간격 mt-11→mt-16(64), 제목 "친구에게", 본문 1줄 카피, 제목색 black
- [x] F05 결과 토스트 → F04/Figma 규격 통일(gray-900/70·px-7·body-14), 게이트 일러스트 박스 270×334 중앙
- [x] F06 인트로 그룹↔일러스트 gap-23(92)·박스 350/300, 완료 gap-16(64)·박스 350/280
- [x] F02 키보드 ON 시 CTA↔키보드 간격 24→12
- ⏸ F05 복사 아이콘: **수정 불필요** — Figma도 `icn_link`(414:13744)라 코드 LinkIcon이 이미 정확(코드 주석이 오기)
- ⏸ F01 제목 line-height: **수정 불필요** — 토큰 lh 1.4(36.4px)가 Figma 27px줄+10px간격(≈37px 베이스라인)과 사실상 동일
- ⏸ F06 인트로 하단 여백(51px)·F02 좌측 1px(19): off-grid Figma 값 → arbitrary 회피, DSGN 확인 대기

**디자이너에게 요청할 것**
- 제목 색 통일(F01 gray-900 ↔ F04 순수 black), ④칸 라벨 Figma 정리("아직 모르는 나")
- radius 토큰 스케일 정의(현재 Figma에 radius/kakao Variable 없음)
- 복사·공유 버튼 테두리색 통일(gray-200 ↔ gray-100)
- 소수점 top 누적(로고 23.52 비율) 정수 스냅 여부

**리포트 대비 해소된 항목** (현 Figma 최신본 기준)
- 제목 색 "#2d8cff·순수 black" → F01은 gray-900/blue-500로 정합 / yellow 토큰 Figma에 정의됨 / 실패 캐릭터 259.5→260 정수화

---

## 0. 기준선 — 팔레트 / 토큰 대조

### ✅ 정합 확인됨 (팔레트)
- `white #FFFFFF`, `gray/50 #F5F6FB`, `gray/100 #E5E7EF`, `gray/200 #C2C6D1`, `gray/300 #828794`, `gray/400 #626670`, `gray/700 #40444D`, `gray/800 #272E3F`, `gray/900 #0F172A` — globals.css와 전부 일치
- `blue/100 #D2E2FF`, `blue/200 #9BBEFF`, `blue/300 #8AB3FF`, `blue/400 #659BFF`, `blue/500 #4E8CFF` — 일치
- `red/300 #FF5858`, `pink/300 #FF63B3` — 일치
- `yellow/200 #FFF4C1`, `yellow/800 #7B7453` — **일치** (리포트는 "Figma에 없음"이라 했으나 현재 Figma Variable에 정의됨 → 리포트 항목 해소)
- 타이포 토큰: head-point1(16/18/20/24/26), head-point2/14, body(14/16/18 × regular/medium/semibold), caption/12 — letterSpacing head -2%/body -3%/caption -2% 모두 코드와 일치

### 검토 대상 (토큰)
- [ ] 🟥 DSGN — **순수 black 토큰화 합의 필요**: Figma에 `Colors/Base/Base Black #000000` 존재하나, 우리 화이트리스트(globals.css)엔 black 없음. F01 제목이 black이면 토큰 추가할지/gray-900으로 흡수할지 결정 요망
- [ ] 🟥 DSGN — **Dimmed 토큰 미정의 측 확인**: Figma `Colors/Dimmed/Dimmed 50 #00000080` 존재. 토스트/딤 배경에 쓰는 토큰인지 확인 → 쓰면 코드에 `--color-dimmed-50` 신설 필요
- [ ] 🟥 DSGN — **이질 gray 토큰 정리**: Figma에 `Colors/Gray Scale/Gray 300 #d0d5dd`, `cool gray/gray800 #3F454A`가 우리 `gray/300 #828794`와 별개로 섞여 있음. 실제 화면에서 쓰이면 우리 토큰으로 통일 요청
- [ ] 🟦 FE — **kakao·radius·shadow 토큰**: 코드에서 임시 신설(`--color-kakao #fee500`, `--radius-field 12px`, `--radius-cta 16px`, `--shadow-bar`). Figma Variable엔 여전히 radius/kakao 미정의 → 디자이너 정의 시 동기화 (리포트 항목 유지)

---

## F01 · 온보딩 / 시작 화면
> Figma `589:3623` (390×844) ↔ `src/app/page.tsx` + `CenteredScreen` + `Cta` + `Logo` + `BgCloud`

### ✅ 정합 확인됨
- 제목 색 `text-gray-900 #0F172A`, "네컷" `text-blue-500 #4E8CFF` — Figma와 일치 (**리포트의 "제목 순수 black·네컷 #2d8cff" 지적은 현 Figma 기준 무효 — 해소**)
- CTA: `h-14`(56) · `rounded-cta`(16) · `bg-gray-900` · `text-head1-18 font-display1` · w350(px-5 컨테이너) — Figma `589:3626`와 일치
- 로고: `<Logo>` 기본 md = `h-7 w-logo-md`(100×28) — Figma logo 인스턴스(100×28) 일치
- 콘텐츠 간격: 로고↔제목 `gap-8`(32), 제목↔캐릭터 `gap-21`(84) — Figma 오토레이아웃 32/84 일치
- 캐릭터: `w-70`(280) × h384 — Figma `362:2151`(280×384) 일치
- 배경: `bg-sky-gradient`(white 21.059%→blue-200) + BgCloud `top-29`(116) — Figma grad/background_blue·bg_cloud(top116) 일치

### 🟦 FE — 프론트 업데이트
- [ ] **제목 line-height 불일치** — 코드 `text-head1-26`는 토큰 line-height `1.4`(=36.4px/줄) ↔ Figma F01 제목 인스턴스는 `leading-[27px]`(≈1.04) + 두 줄 사이 `mb 10px`. 2줄 히어로 제목이 코드에서 ~9px/줄 더 벌어짐. → Figma처럼 줄간격 27px로 맞출지 결정 (아래 DSGN 항목과 연계)

### 🟥 DSGN — 디자이너 수정요청 / 확인
- [ ] **head-point1/26 토큰 line-height 모순** — Figma Variable `head-point1/26` 정의는 lineHeight `1.4`인데, F01 제목 실제 인스턴스는 `27px`(≈1.04)로 override돼 있음. 토큰과 사용처가 어긋남 → 제목 줄간격 기준값을 토큰(1.4)으로 갈지 인스턴스(27px)로 갈지 통일 요청

### 📝 참고
- 콘텐츠 세로 위치: 코드는 `flex-1` 스페이서로 **세로 중앙 정렬**(화면 높이 가변, 844 기준 top≈96) ↔ Figma는 고정 `top 104`. 약 8px 차이지만 의도된 반응형 설계(CenteredScreen). 디자인이 고정 위치를 요구하면 재논의.
- 리포트 "CTA 아래 여백 34"는 F01 프레임에서 재현 안 됨 — Figma `589:3625` 하단 패딩 `pb 24` = 코드 `pb-6`(24) 일치. (34 값은 F02에서 재확인 예정)

---

## F02 · 닉네임 설정
> Figma `414:13301`(진입/키보드On) · `414:13283/13292/13332`(키보드Off) ↔ `onboarding/nickname/page.tsx` + `useKeyboardInset` + `TextfieldSet`/`Textfield`

### ✅ 정합 확인됨 — 디자인팀 요청 2건 **구현 완료**
- **[요청] 화면 진입 시 키보드 활성화** — `inputProps.autoFocus: true`. Figma 진입화면(414:13301)이 키보드 ON 상태와 일치. (iOS Safari 프로그램적 포커스 한계 주석 있음 — 플랫폼 제약)
- **[요청] 키보드 따라 버튼 올라오기** — `useKeyboardInset()`(visualViewport) + CTA `translateY(-inset)`. Figma 진입화면 CTA가 키보드 바로 위(top 444)에 붙는 설계와 일치.

### ✅ 정합 확인됨 — 레이아웃/스타일
- 로고 **sm**(`h-6 w-logo-sm` 84×24) — Figma F02 logo 84×23.52 일치 (F01의 md와 구분 정확)
- 상단 여백 `pt-9`(36) — Figma 콘텐츠 top 80(상태바 44 포함) → 웹 환산 36 일치
- 제목 `text-head1-24 font-display1 gray-900` / 본문 `text-body-16-medium gray-300` — Figma head-point1/24·body/16-medium·gray/300 일치
- 간격: 로고↔제목 `mt-8`(32) / 제목↔본문 `gap-3`(12) / 제목블록↔입력칸 `mt-9`(36) — Figma 32/12/36 일치
- 입력칸 `h-15`(60)·`rounded-field`(12)·`border-gray-200`·`px-4`(16) — Figma textfield 350×60 r12 일치
- CTA 하단 여백 `pb-6`(24) — Figma 키보드Off CTA 하단 24 일치 (**리포트 "CTA 아래 여백 34"는 현 Figma에서 재현 안 됨 — 전 F02 프레임 24px**)
- 닉네임 길이 8자 제한 — 디자이너 확정값 반영됨

### 🟦 FE — 프론트 업데이트 (px 미세)
- [ ] **키보드 ON 시 CTA↔키보드 간격** — 코드는 `pb-6`(24) 때문에 키보드 위 24px 뜸 ↔ Figma 진입화면은 키보드 위 12px. 12px 차이. 진입화면 한정으로 간격 줄일지 검토
- [ ] **콘텐츠 좌측 여백 1px** — 코드 `px-5`(좌20) ↔ Figma 콘텐츠 프레임 x=19(좌19). 1px. (Figma 19px가 4px 그리드 밖 — DSGN 확인 필요)

### 🟥 DSGN — 확인
- [ ] **소수점 top 값(.52) 누적 원인** — 리포트 "제목블록 top 138 / 입력칸 top 269"는 Figma 135.52 / 266.52와 사실상 동일. 이 .52는 **로고 인스턴스 높이 23.52(=84×28/100 비율)** 에서 파생돼 하위 요소 top에 연쇄됨. 그리드 정렬하려면 로고 높이/위치를 정수로 스냅 필요 — 디자이너 판단 요청

---

## F03 · 내 설문 (게시자) — 설문 진행기 (참여자 F06과 공용)
> Figma `414:13343`(+13361/13384 변형) ↔ `survey-runner.tsx` + `BtnSurvey` + `ProgressBar` + `onboarding/survey/page.tsx`

### ✅ 정합 확인됨
- 배경 `bg-gray-50 #F5F6FB` — Figma 일치
- 헤더 `h-15`(60)·`px-2`(8) / 뒤로 버튼 터치영역 `size-12`(48)·아이콘 `size 24`(left12) — Figma header h60·icn-arrow 48/24 일치 (개발 주석 "터치영역 48×48"까지 반영)
- 진행바: `h-1`(4)·트랙 `bg-white`·`rounded-none`·채움 `bg-blue-500` 1/8(48.75px) — Figma progress_bar h4 흰트랙·blue fill 일치
- "n / 8": "n" `text-blue-500`, "/ 8" `text-gray-300`, `text-body-16-medium` — Figma 색/타이포 일치
- 지문: `text-head1-24 font-display1 text-gray-900` + `line-clamp-3 break-keep` — Figma head-point1/24 + "최대 3줄·단어단위 줄바꿈" 주석 정확 반영
- 간격: "n/8"↔지문 `gap-2`(8) / 지문블록↔보기 `mt-12`(48) / 보기 간 `gap-4`(16) — Figma 8/48/16 일치
- 보기 버튼 default(`bg-white text-gray-400 pl-4 pr-5 h-16 rounded-field`) / activated(`bg-gray-800 text-white px-5`) — Figma btn_survey default/activated px·색 일치
- 상단 서브젝트 라벨 미렌더 — Figma F03에도 없음(일치)

### 🟦 FE — 프론트 업데이트 (px 미세)
- [ ] **진행바 채움 모서리** — `ProgressBar` 채움 바가 `rounded-full`인데 트랙은 `rounded-none`(override). Figma 채움 `bar..?`는 사각(모서리 0). 4px라 미세하나 채움도 사각 처리 권장 (공용 컴포넌트라 share-view 게이지와 함께 영향 — F04에서 재확인)

### 📝 참고
- 보기 버튼 절대 top 329(Figma)는 4px 그리드 밖 — 코드는 상대간격(mt-12=48)으로 재현하므로 지문 줄수에 따라 위치 가변. (그리드 정렬은 디자이너 지문 블록 높이 고정 시 해소)

---

## F04 · 공유 링크 발급 (수집 중)
> Figma `602:6556`(메인) · `627:9607`(토스트알림) ↔ `share-view.tsx` + `ShareCards`(share-cards.tsx) + `Cta`/`CtaSmall`

### ✅ 정합 확인됨 — 디자인팀 요청 2건 **구현 완료**
- **[요청] 복사·인스타 동일 토스트** — 토스트 `"링크 복사 완료!"`가 복사 버튼과 인스타 스토리(shared/copied) 양쪽 공통(`copyDoneToast`). Figma `627:9624` 토스트 + dev주석("두 버튼 모두 동일 텍스트")과 일치. 스타일도 `bg-gray-900/70`·`px-7 py-2`·`rounded-full`·`text-body-14-medium text-white` = Figma rgba(15,23,42,.7)·px28/py8·r50·Pretendard14 white 정합
- **[요청] 인디케이터 gray/50** — 비활성 점 `bg-gray-50`(w1.5 h1.5), 활성 `bg-gray-900`(w4 h6 pill), 간격 `gap-1.5`(6) — Figma indicatorbar(active #0F172A 16×6 / dot gray-50 6×6 / gap6)와 정합. (커밋 43b3ef5 반영 확인)

### ✅ 정합 확인됨 — 레이아웃/컴포넌트
- 로고 sm(84×24), 상단 `pt-9`(36)=Figma top80(−상태바44), 로고↔제목 `mt-8`(32), 제목↔본문 `gap-3`(12) — 일치
- 안내 카드: `rounded-[20px]`·`backdrop-blur-[10px]`·`bg-white/40`·`border-white`·`h-76`(304=Figma pt20+badge33+16+char148+16+말풍선47+pb24)·내부 `gap-4`(16) — 일치
- 카드 배지: `bg-blue-100 rounded-md`(8)·`px-3 py-1`·`font-display2 text-head2-14 text-blue-500` — Figma blue-100/r8/px12py4/YPairing14 일치
- 말풍선: `rounded-full bg-white px-4 py-2 text-body-14-medium text-gray-700` — 일치 / 카드 카피("1. …내 링크를 꼭 복사해줘!") 일치
- 카드↔인디케이터 `gap-4`(16) — 일치
- 큰 CTA(`Cta` head1-18) / 작은 버튼(`CtaSmall` stroke=border-gray-200, fill=bg-kakao, 둘 다 text-gray-900 head1-16, px-5, h-14) — Figma CTA/CTA_small px·색·타이포 일치
- CTA↔작은버튼 `gap-2`(8), 하단 `pb-6`(24) — 일치

### 🟦 FE — 프론트 업데이트
- [ ] **제목블록→카드 간격 20px 부족** — 코드 `mt-11`(44) ↔ Figma(602:6556·627:9607 양 프레임 모두) **64px**. → `mt-16`(64)로 수정. (코드 주석은 "44px"로 잘못 기재돼 있음)
- [ ] **제목 카피 불일치** — 코드 `"친구들에게 링크를 공유하고 / 네컷을 받아보세요!"` ↔ Figma `"친구에게 링크를 공유하고 / 네컷을 받아보세요!"` ("친구들에게"→"친구에게")
- [ ] **본문 카피·줄수 불일치** — 코드 `"친구들의 답이 모이면, / 24시간 뒤 나를 담은 네컷 결과가 열려요"`(2줄) ↔ Figma `"답이 모이면, 24시간 뒤 네컷 결과가 열려요"`(1줄, 더 간결). → Figma 카피로 통일 (또는 DSGN 최종본 확인)
- [ ] **제목 색** — 코드 `text-gray-900`(#0F172A) ↔ Figma F04 제목 `text-black`(순수 #000000). (아래 DSGN과 연계 — black 토큰화 또는 gray-900 통일)

### 🟥 DSGN — 디자이너 수정요청 / 확인
- [ ] **제목 색 페이지 간 불일치** — F01 제목은 `gray/900 #0F172A`인데 **F04 제목은 순수 black #000000**. 같은 위계 제목인데 Figma에서 색이 다름. 한쪽으로 통일 요청(팔레트에 있는 gray-900 권장). [[기준선-순수-black]]
- [ ] **최종 카피 확정** — F04 제목/본문 문구가 코드와 Figma가 서로 다름. 어느 쪽이 최신인지 확정 필요

### 📝 참고
- `CtaSmall`에 `stroke_icn`(LinkIcon 포함) variant가 정의돼 있으나 F04에서 미사용(인스타 버튼은 텍스트 전용 `stroke`) — 현 Figma F04도 아이콘 없음(일치). 리포트의 "공유 화면 링크 아이콘+gray-200 테두리"는 현 F04에 해당 요소 없음 → **복사버튼 일관성은 F05 결과화면과의 대비 문제로 재정의** (F05에서 확인)

---

## F06 · 친구 설문 응답 (인트로 / 설문 / 완료)
> Figma 인트로 `414:13450` · 설문 `414:13472/13490/13513` · 완료 `414:13540` · **추가 `414:13565`** ↔ `respondent-view.tsx` + `SurveyRunner`

### ✅ 정합 확인됨
- **인트로**: sky 배경, 로고 md(100×28, 중앙), 제목 `text-head1-26`("{nickname}님은 / 어떤 사람인가요?", nickname blue-500), 로고↔제목 `gap-12`(48), 2초 자동전환 — Figma 일치. (※ 여기선 head1-26 lh1.4=72px가 Figma 제목 h70과 맞음 → F01 제목 line-height 문제와 대조)
- **완료**: sky 배경, 로고 md 중앙, 제목 `text-head1-24`("…답변을 전했어요!"), 제목↔서브 `gap-3`(12), 로고↔제목블록 `gap-10`(40), 안내문("남이 본 내 모습이…" body-14-medium gray-800)↔CTA `gap-3`(12), CTA `pb-6`(24), "나도 만들기"→`/`(바이럴 루프) — Figma 일치. 카피 전부 일치
- **설문 ×3**: `SurveyRunner` 재사용 → F03에서 검증한 헤더/진행바/지문/보기 그대로 적용

### 🟦 FE — 프론트 업데이트
- [ ] **인트로 일러스트: Figma에 실제 에셋 있음** — Figma 인트로는 `img_character_hamster_star`(350×300) 실 일러스트 ↔ 코드는 `"[GIF] 3,2,1 카운트다운 캐릭터 삽입 예정"` 회색 플레이스홀더. 에셋 교체 필요(+카운트다운 GIF인지 정적 별햄스터인지 컨셉 확인). 🙋 에셋은 사용자 진행
- [ ] **인트로 그룹↔일러스트 간격** — 코드 `gap-14`(56) ↔ Figma 92px. 36px 부족
- [ ] **인트로 일러스트 박스 비율** — 코드 `aspect-[350/332]` ↔ Figma 350×300
- [ ] **인트로 하단 안내문 여백** — 코드 footer `pb-8`(32) ↔ Figma 안내문 하단 ~51px. 약 19px 차이
- [ ] **완료 그룹↔일러스트 간격** — 코드 `gap-10`(40) ↔ Figma 64px. 24px 부족
- [ ] **완료 일러스트 박스 비율** — 코드 `aspect-[350/332]` ↔ Figma 350×280

### 🟥 DSGN / PM — 확인
- [ ] ~~미구현 화면 414:13565~~ → **정정**: `414:13565`는 미구현이 아니라 **F05 결과 게이트 화면**(result-view `!entered`)으로 구현돼 있음. Figma가 F06 프레임 그룹에 배치했을 뿐, 실제론 主人公의 결과 진입 화면. → F05 섹션에서 검증함
- [ ] **F06 설문 Q1 뒤로 화살표** — Figma 설문 Q1(414:13472)엔 `icn-arrow-left`(48 터치영역) 있음 ↔ 코드는 Q1에서 숨김(splash 자동전환이라 돌아갈 곳 없음 판단). Q1에서 뒤로가면 어디로 갈지 정의 필요(splash 재진입 vs 숨김 유지)

---

## F05 · 결과 (게이트 / 인생네컷 본문 / 실패 / 스켈레톤)
> Figma 게이트 `414:13565` · 네컷 본문 `414:13631`(390×2923) · 실패 `414:13591` · 스켈레톤 `627:6590`(390×2908) ↔ `result-view.tsx` + `retry-view.tsx`

### 🔴 BIG — 결과 본문이 Figma 풀디자인과 구조적으로 다름 (최우선 의사결정 필요)
- [ ] **결과 본문 간소화 ↔ Figma 풀디자인** — 코드 result-view(entered)는 `2×2 이미지 그리드 + 칸별 interpretation + 디스클레이머 + 공유바`로 **간소화**. 코드 주석: *"백엔드가 칸별 imageUrl+interpretation 4칸만 제공 → headline/형용사/summary/tip/합성네컷 제거"*.
  Figma 본문(`414:13631`, 2923px)은 전혀 다른 구조:
  - `4cuts_frame`(358×552) — 프레임 두른 합성 네컷 카드 (report: radius18·안쪽14·칸간격10)
  - `result`(350×255) — 종합 헤드
  - `result_detail`(350×1364) — **형용사 태그 + 칸별 상세 설명** (report: 태그 radius25·간격7)
  - `tip`(350×156) — 팁 카드 (report: radius14)
  - `btm_CTA_area` — CTA + CTA_kakao
  → **PM/백엔드/디자이너 합의 필요**: 간소화 버전이 MVP 확정인지, Figma 풀디자인으로 구현할지. 백엔드가 형용사/종합분석/팁을 제공하는지에 종속. **리포트의 F05 상세 수치(358 카드·형용사 태그 등)는 전부 이 "미구현 풀디자인" 대상** → 현재 코드엔 해당 요소 없음
- [ ] **결과 스켈레톤 미구현** — Figma `627:6590`(2908px) 결과 스켈레톤 디자인 존재 ↔ 코드는 단순 스피너("네컷을 만들고 있어요"). (위 간소화 결정에 종속)

### ✅ 정합 확인됨 — 결과 게이트 (`!entered`, Figma 414:13565)
- sky 배경, 로고 md(100×28 중앙), 제목 `text-head1-24`("{nickname}님의 네컷이 / 완성됐어요"), 서브("친구 {n}명의 응답을 보러 갈까요?" body-16-medium gray-300) — 카피/타이포 일치
- 간격: 로고↔제목블록 `gap-14`(56)=Figma 56, 제목↔서브 `gap-3`(12)=Figma 12, 그룹↔일러스트 `gap-14`(56)≈Figma 54(2px) — 일치
- CTA "내 네컷 결과 보기" + `setEntered(true)` 전환

### ✅ 정합 확인됨 — 결과 실패 (retry-view, Figma 414:13591)
- gray 배경(`background="gray"`) + BgCloud — Figma 회색 그라데이션 일치
- 로고 md 중앙(274 폭 그룹), 제목 `text-head1-24`("앗! 3명이 모이지 않았어요.."), 서브 `text-body-18-medium gray-300`("다시 시작해 볼까요?")
- 간격: 로고↔제목블록 `gap-14`(56), 제목↔서브 `gap-3`(12), 그룹↔캐릭터 `gap-20`(80), 안내문↔CTA `gap-10`(40), CTA `pb-6`(24) — Figma 56/12/80/40/24 전부 일치
- 캐릭터 `character-sad` **204×260** — Figma `img_character_hamster_sad` 204×260 일치 (**리포트 "204×259.5 소수점"은 현 Figma에서 260으로 해소**)
- 카피 전부 일치

### 🟦 FE — 프론트 업데이트 (게이트/공통)
- [ ] **게이트 일러스트 규격** — Figma `img_character_hamster_insight_noword` 270×334(중앙) ↔ 코드 박스 `aspect-[350/332]`(전체폭). 실 에셋 적용 시 270 폭 중앙 배치. 🙋 에셋은 사용자 진행
- [ ] **결과 토스트가 F04 토스트와 다름** — 코드 result 토스트 `bg-gray-900`(불투명)·`px-4 py-2`·`text-caption-12-medium`·문구 "링크가 복사됐어요!" ↔ F04 토스트 `bg-gray-900/70`·`px-7 py-2`·`text-body-14-medium`·"링크 복사 완료!". **두 토스트 스타일·문구 불일치** → Figma 토스트 규격(F04 기준, rgba70%/px28/body14)으로 통일
- [ ] **결과 복사 버튼 아이콘** — 코드 `stroke_icn`이 **link 아이콘** 내장 ↔ Figma 결과 복사 버튼은 **copy 아이콘**. copy 아이콘 SVG로 교체 (코드 주석에 "2단계 교체 예정" 명시)

### 🟥 DSGN — 디자이너 수정요청 / 확인
- [ ] **복사/공유 버튼 테두리색 페이지 간 불일치** — F04 `CtaSmall stroke`는 `border-gray-200`, F05 결과 공유바/다운받기/디스클레이머는 `border-gray-100`. 같은 위계 보조버튼인데 화면마다 테두리 농도가 다름 → 통일 요청 (리포트 "복사 버튼 화면마다 다름"의 실체)
- [ ] **④칸 라벨 Figma 정리** — 코드 `data/quadrants.ts`는 `unknown="아직 모르는 나"`로 단일화 완료 ✓. 그러나 Figma 네컷/배지엔 "아무도 모르는 나"/"아무도 모르는" 잔존(리포트). Figma 라벨을 "아직 모르는 나"로 통일 요청 [[기준선]]
- [ ] **CTA_kakao 컴포넌트** — Figma `btm_CTA_area`에 `CTA_kakao`(173×56) 별도 컴포넌트(카카오 심볼 포함 가능) ↔ 코드는 `CtaSmall fill`(텍스트만). 카카오 심볼 노출 여부 확인 (KakaoIcon 에셋 준비됨, 미사용)

---

## 공통 · 토큰 / 컴포넌트 정합

### ✅ 정합 확인됨
- **Logo**: sm(84×24)/md(100×28) 2종, viewBox 100:28 비율 고정, `currentColor`+`text-blue-500` 토큰화 — 화면별 sm/md 구분 정확(F02·F03·F04·결과본문 헤더=sm / F01·F06·게이트·실패=md)
- **아이콘**: arrow-left(24, gray-900), download(24), star 등 `currentColor` 상속 토큰화 — F03 헤더 화살표 48 터치영역/24 아이콘 검증됨
- **타이포 토큰**: head-point1/2·body·caption 전 스케일 Figma Variable과 letterSpacing까지 일치
- **배경 그라데이션**: `bg-sky-gradient`/`bg-gray-gradient` Figma grad/background_blue·_gray와 일치

### 🟥 DSGN + 🟦 FE — radius 스케일 정리 (리포트 공통 항목)
- [ ] **radius 값 산재** — Figma 전 화면에서 `8·12·14·16·18·20·25·42·50`px 혼재. 코드도 토큰(`field 12`/`cta 16`) + shadcn 스케일(sm~4xl) + raw(`rounded-[20px]`, `rounded-full`) 혼용. → **radius 토큰 스케일 합의**(예: xs8/sm12/md14/lg16/xl18/2xl20/pill) 후 Figma Variable 등록 + 코드 토큰화. (현재 Figma엔 radius Variable 자체가 없음 — 코드 임시 신설 상태)

### 🟥 DSGN — 네컷 첫 칸 테두리 투명도 (리포트 공통, 풀디자인 대상)
- [ ] **4cuts 첫 칸 테두리 28% ↔ 나머지 15%** — Figma 네컷 카드(414:13631 4cuts_frame)에서 첫 칸만 테두리 투명도가 달라 튐. (단 코드는 네컷 카드 미구현 — 풀디자인 채택 시 함께 정리)

### 📝 만료/없는 링크 (ExpiredView)
- Figma 프레임 없음(product-spec §8 스텁, 디자인 대기). 코드는 gray 배경 + 로고 md + "링크를 찾을 수 없어요" + "내 것 만들기"로 자체 구성. 디자인 나오면 대조 필요. 라우팅 상태머신([token]/page.tsx)은 share/respondent/result-gate/retry/expired 정상 분기 확인.

---

## 디자인팀 → 프론트 요청 4건 — 반영 현황

| # | 요청 | 상태 | 근거 |
|---|------|------|------|
| 1 | 내 링크 복사 / 인스타 스토리 공유 시 **동일 토스트** | ✅ **완료** | `share-view.tsx` `copyDoneToast="링크 복사 완료!"` 복사·인스타 공통. Figma 627:9624 dev주석과 일치 |
| 2 | 화면 진입 시 **키보드 활성화 + 버튼 따라 올라오기** | ✅ **완료** | `nickname` `autoFocus` + `useKeyboardInset`+`translateY`. (iOS 프로그램적 포커스 한계 주석) |
| 3 | **인디케이터 컬러 gray/50** | ✅ **완료** | `share-cards.tsx` 비활성 점 `bg-gray-50`. 커밋 43b3ef5 |
| 4 | 에셋 전체 3배수/4배수 재다운로드 | 🙋 **사용자 진행 예정** | 화질 개선용. 인트로/게이트/네컷 일러스트 교체와 함께 진행 |

> ⚠️ 요청 1(토스트)은 F04에선 완료지만 **F05 결과 토스트는 스타일·문구가 달라 통일 필요**(F05 FE 항목 참조). "동일 토스트"를 전 화면 공통 컴포넌트로 뽑으면 깔끔.

---
*검토 완료: F01~F06 + 공통/토큰. 자동 수정 없음 — 위 체크박스는 합의·작업용.*

---

## 🔁 재검토 — main `99a85c2` pull 후 (2026-06-24, 에셋 교체 반영 시점)

> 다른 세션에서 일러스트 실 에셋(4x) 교체·연결 완료(커밋 `99a85c2`). placeholder/더미 잔존 재점검 결과.

### ✅ 확인됨 (깨끗함)
- **Placeholder 박스 0건** — `aspect-[]` 회색 박스 전부 사라짐. 인트로/완료/결과게이트 일러스트가 실 이미지로 교체됨 (`img_character_hamster_star` / `_letter` / `_insight_noword`)
- **더미데이터 없음** — 전 화면 실 API 훅. `김루키` 등 하드코딩 닉네임 없음. 데모 스위처·`?view=` 이미 제거됨(`[token]/page.tsx` 주석)
- `ShareCards`의 `CARDS`(1·2·3단계)는 더미 아닌 **실 콘텐츠** / `style-guide`의 `SAMPLE`은 개발용 레퍼런스 → 유지
- 결과 빈 칸 🌫️ "아직 다 발견하지 못했어"는 도메인상 **정상 빈 상태** → 유지
- public/assets 전 파일 사용 중 (1~3 참조), `bg_cloud.png`로 통일됨

### 🧹 정리 후보 (이어서 작업할 것 — 미수정 상태)
- [ ] **`public/assets/result.png` 삭제** — 전 저장소 미참조. 848KB 와이어프레임 잔재(유일한 미사용 에셋). *(삭제 전 다른 세션 에셋작업과 안 겹치는지만 확인)*
- [ ] **다운받기 버튼 결정** (`result-view.tsx` L160~169) — `onClick` 없는 **비기능 placeholder**. ⓐ네컷 캡처 다운로드 구현 / ⓑ기능 나올 때까지 숨김 / ⓒ유지 중 택1. (네컷 본문 풀디자인 의사결정과 함께 다루면 됨)
- [ ] **stale 주석 삭제** (`share-view.tsx` L20) — "중앙 일러스트는 디자이너 별도 프레임 대기 → placeholder만" → 실제론 placeholder 없이 `ShareCards` 렌더. 주석만 정리
- [ ] **미사용 prop 제거** (`survey-runner.tsx`) — `subjectLabel` 데드코드(현재 미렌더). 정리 또는 용도 확정
- [ ] (참고) `share-view.tsx` TODO "수집 게이지·카운트다운(응답수/남은시간) 미구현" — 더미 아님. **product-spec #4 미구현 기능**(별개 이슈, status의 peerSubmissionCount/remainingSeconds로 추가 가능)

### 📝 비고
- 검토 시점 working tree 깨끗(다른 세션 미커밋 변경 없음). 위 항목은 전부 **미수정** — 이어서 진행 시 이 목록부터.
